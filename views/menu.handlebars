{{!-- featured item --}}
{{!-- have to make header --}}
<section class="card">
  <a href="/menu/{{id}}">
    <header>{{name}}</header>
    <div class="card-body">
      <img src="/images/{{filename_name}}" alt="{{image_description}}">
      <p>Ingredients {{ingredients}}</p>
      <p>Directions {{directions}}</p>
    </div>
  </a>
  {{!-- main menu --}}